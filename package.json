{
  "name": "stockpile",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "setup": "bun scripts/setup.ts",
    "api:dev": "sst shell -- bun --watch apps/api/index.ts",
    "db:generate": "sst shell -- drizzle-kit generate",
    "db:migrate": "sst shell -- drizzle-kit migrate",
    "stories:ingest": "sst shell -- bun apps/api/scripts/ingest-stories.ts",
    "stories:recontext": "sst shell -- bun apps/api/scripts/recontext-stories.ts",
    "stories:images": "sst shell -- bun apps/api/scripts/backfill-story-images.ts",
    "tokens:coverage": "sst shell -- bun apps/api/scripts/check-tokens-coverage.ts",
    "xstocks:probe": "sst shell -- bun apps/api/scripts/probe-xstocks.ts",
    "prices:snapshot": "sst shell -- bun apps/api/scripts/snapshot-prices.ts",
    "congress:ingest": "sst shell -- bun apps/api/scripts/ingest-congress.ts",
    "test:backend": "sst shell -- bun test tests/backend",
    "generate:sdk": "bun run --cwd apps/api generate:openapi && bun run --cwd packages/api-client generate"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "functions"
  ],
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260424.1",
    "@types/bun": "latest",
    "@types/pg": "^8.15.6",
    "drizzle-kit": "^0.31.7"
  },
  "peerDependencies": {
    "typescript": "5"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.7",
    "pg": "^8.16.3",
    "sst": "^4.12.2"
  }
}
